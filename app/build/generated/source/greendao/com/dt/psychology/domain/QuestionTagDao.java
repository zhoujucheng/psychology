package com.dt.psychology.domain;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "QUESTION_TAG".
*/
public class QuestionTagDao extends AbstractDao<QuestionTag, Long> {

    public static final String TABLENAME = "QUESTION_TAG";

    /**
     * Properties of entity QuestionTag.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property QuestionTagId = new Property(0, Long.class, "questionTagId", true, "_id");
        public final static Property QuestionTagName = new Property(1, String.class, "questionTagName", false, "QUESTION_TAG_NAME");
    }

    private DaoSession daoSession;


    public QuestionTagDao(DaoConfig config) {
        super(config);
    }
    
    public QuestionTagDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"QUESTION_TAG\" (" + //
                "\"_id\" INTEGER PRIMARY KEY ," + // 0: questionTagId
                "\"QUESTION_TAG_NAME\" TEXT);"); // 1: questionTagName
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"QUESTION_TAG\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, QuestionTag entity) {
        stmt.clearBindings();
 
        Long questionTagId = entity.getQuestionTagId();
        if (questionTagId != null) {
            stmt.bindLong(1, questionTagId);
        }
 
        String questionTagName = entity.getQuestionTagName();
        if (questionTagName != null) {
            stmt.bindString(2, questionTagName);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, QuestionTag entity) {
        stmt.clearBindings();
 
        Long questionTagId = entity.getQuestionTagId();
        if (questionTagId != null) {
            stmt.bindLong(1, questionTagId);
        }
 
        String questionTagName = entity.getQuestionTagName();
        if (questionTagName != null) {
            stmt.bindString(2, questionTagName);
        }
    }

    @Override
    protected final void attachEntity(QuestionTag entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public QuestionTag readEntity(Cursor cursor, int offset) {
        QuestionTag entity = new QuestionTag( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // questionTagId
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1) // questionTagName
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, QuestionTag entity, int offset) {
        entity.setQuestionTagId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setQuestionTagName(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(QuestionTag entity, long rowId) {
        entity.setQuestionTagId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(QuestionTag entity) {
        if(entity != null) {
            return entity.getQuestionTagId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(QuestionTag entity) {
        return entity.getQuestionTagId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
